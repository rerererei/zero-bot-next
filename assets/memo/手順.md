# ZERO-bot EC2 デプロイ完全手順（保存用）

## 🚀 1. EC2 インスタンス作成

* OS: Amazon Linux 2023
* インスタンスタイプ: t3.micro
* ストレージ: 20GB（GP3）
* パブリックIPv4アドレスを割り当てる
* セキュリティグループ:

  * SSH (22) を自分のIPからのみ許可

---

## 🚀 2. SSH 接続

```bash
ssh -i ~/.ssh/zero-bot.pem ec2-user@＜パブリックIP＞
```

---

## 🚀 3. パッケージ更新

```bash
sudo dnf update -y
```

---

## 🚀 4. Python / Git インストール

```bash
sudo dnf install python3.9 python3.9-pip git -y
```

---

## 🚀 5. ZERO-bot リポジトリ取得

```bash
git clone https://github.com/yourname/zero-bot-next.git
cd zero-bot-next
```

---

## 🚀 6. 仮想環境作成

```bash
python3.9 -m venv venv
source venv/bin/activate
```

---

## 🚀 7. ライブラリインストール

```bash
pip install -r requirements.txt
```

---

## 🚀 8. .env 作成

```bash
nano .env
```

必要な環境変数を貼り付け → `Ctrl + O` → Enter → `Ctrl + X`

---

## 🚀 9. 起動テスト

```bash
python main.py
```

成功が確認できたら、`Ctrl + C` で停止。

---

# 🔥 10. systemd による常時起動化

## サービスファイル作成

```bash
sudo nano /etc/systemd/system/zerobot.service
```

内容:

```
[Unit]
Description=Zero Discord Bot
After=network.target

[Service]
User=ec2-user
WorkingDirectory=/home/ec2-user/zero-bot-next
ExecStart=/home/ec2-user/zero-bot-next/venv/bin/python3 main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

## systemd 反映

```bash
sudo systemctl daemon-reload
```

## 自動起動有効化

```bash
sudo systemctl enable zerobot
```

## 起動

```bash
sudo systemctl start zerobot
```

## 状態確認

```bash
sudo systemctl status zerobot
```

## ログ監視

```bash
journalctl -u zerobot -f
```

---

# 🎉 完了！

この手順で EC2 上で ZERO-bot を本番運用できます！
